// .eslintrc.js
module.exports = {
  env: {
    node: true,     // para que ESLint reconozca 'require', etc.
    es2022: true    // sintaxis moderna JS
  },
  extends: ["eslint:recommended"],
  rules: {
    // Tus reglas personalizadas (opcional)
  }
};
